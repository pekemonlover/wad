# Dockerized Website
## Theory
Docker containers package up application code and its dependencies.
## Setup and Run
1. Install Docker.
2. Run `docker build -t my-website .`.
3. Run `docker run -p 8080:80 my-website`.
